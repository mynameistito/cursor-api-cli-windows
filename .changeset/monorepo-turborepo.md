---
"cursor-api-cli-windows": patch
"@cursor-api/web": minor
---

Restructure the repository into a Turborepo monorepo with Bun workspaces. Move the Windows CLI to `apps/cli`, add a TanStack Start documentation site at `apps/web`, and update CI, Changesets, and release tooling for multi-package versioning.
